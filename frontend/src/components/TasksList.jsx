import React from "react";
import classes from("./TaskList.module.css")

const TasksList = () => {
    return <div className={classes[task-container]}>
        Task List
    </div>;
};

export default TasksList;
